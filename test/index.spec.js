/* global describe, it, before */

import chai from 'chai';
import { uploadVideo } from '../lib/SwingbotLicenseeSDK.js';

chai.expect();

const expect = chai.expect;

describe('test getting the signed url', function (done) {
  it('should get the signed url', function (done) {
    done();
  });
});
